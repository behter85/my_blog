<!-- Show all publication's -->
<!-- 
<h1>Listing posts</h1>
 
<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>
 
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.title %></td>
      <td><%= post.text %></td>
       <td><%= link_to 'Show', post %></td>
      <td><%= link_to 'Edit', edit_post_path(post) %></td>
    </tr>    
  <% end %>
  <tr>
    <td>
      <%= link_to 'New post', new_post_path %>
    </td>    
  </tr>
</table> -->

%h1 Listing posts
%table
  %tr
    %th Title
    %th Text
  - @posts.each do |post|
    %tr
      %td= post.title
      %td= post.text
      %td= link_to 'Show', post
      %td= link_to 'Edit', edit_post_path(post)
  %tr
    %td
      = link_to 'New post', new_post_path